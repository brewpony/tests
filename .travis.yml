language: "perl"
perl:
  - "5.16"
before_script:

install:
    - cpanm -v --installdeps --notest --mirror http://cpan.mirrors.travis-ci.org .

script: "perl Build.PL; ./Build test"
# branches:
#   only:
#     - master
notifications:
  recipients:
    - support+tests@brewpony.com
  email:
    on_success: change
    on_failure: change
#env:
